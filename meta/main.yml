---
galaxy_info:
  author: Mullholland
  namespace: mullholland
  role_name: gitlab
  description: Installs and configures Gitlab. Adds an optional backupscript for local backups including config and terraform states.
  license: MIT
  company: none
  min_ansible_version: "2.10"

  platforms:
    - name: EL
      versions:
        - "7"
        - "8"
        # - all  # TODO: CentOS 9 is unreliable
    # should work, but testing is unreliable
    # - name: Amazon
    #   versions:
    #     - Candidate
    - name: Ubuntu
      versions:
        - all
    - name: Debian
      versions:
        - all

  galaxy_tags:
    - development
    - git
    - backup

dependencies: []
